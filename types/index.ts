/**
 * Surplus IQ — shared types barrel.
 *
 * Import from "@/types" across the app:
 *   import { LeadType, type Lead } from "@/types";
 *
 * These definitions mirror `prisma/schema.prisma`. The generated Prisma client
 * lives in `lib/generated/prisma` and is the runtime source of truth; these
 * are the portable, dependency-free domain types.
 */

export * from "./enums";
export * from "./models";
